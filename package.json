{
	"name": "shipkit",
	"version": "1.0.0",
	"private": true,
	"description": "One pipeline for shipping iOS apps: research → build → ship → grow",
	"type": "module",
	"bin": { "ship": "./bin/ship" },
	"engines": { "node": ">=20" },
	"files": ["bin", "src", "mcp", "skills", "templates", "ci"],
	"scripts": {
		"test": "node --test test/*.test.mjs",
		"lint": "npx --yes oxlint@latest src bin",
		"check": "npm run lint && npm test"
	},
	"dependencies": {}
}
